import styles from '../style.module.scss';
const Bottom = () => {
  return (
    <div className={`absolute bottom-2 right-[280px] left-[280px] mx-4 h-40 ${styles.panel}`}>
      Left
    </div>
  );
};

export default Bottom;
